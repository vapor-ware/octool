#!/usr/bin/env groovy

// Include this shared CI repository to load script helpers and libraries.
library identifier: 'vapor@1.15.3', retriever: modernSCM([
  $class: 'GitSCMSource',
  remote: 'https://github.com/vapor-ware/ci-shared.git',
  credentialsId: 'vio-bot-gh',
])

golangPipeline([
  'image': 'vaporio/octool',
  'skipIntegrationTest': true,
  'skipUnitTest': true,
  'skipSetup': true,
  'skipLint': true,
  'goVersion': '1.16',
  'mainBranch': 'master',
])
